# 更新记录

## 0.1.0-rc.2

- 新增 `full`、`core`、`incompatible` 自动分层兼容和 `DIA_ZH_MODE=core`。
- 状态升级为 schema v2，记录补丁版本、源版本/Build、覆盖模式、备份、菜单动态库
  哈希和时间，并安全处理官方更新后的旧状态。
- 将菜单源码、arm64 构建和加载配置纳入安装流程。
- 统一 408 条词表与 156 个菜单键，消除重复译文来源并保护用户内容。
- 重写嵌套签名流程，保留 Renderer/GPU JIT 权限并修复 Library Validation Code 6。
- 新增 manifest 索引、签名 Fixture、碰撞测试、失败回滚、完整/核心模式与恢复验收；
  冒烟检查失败时会显式恢复官方备份。
- 核心模式已在 Dia `1.44.1 (85212)` 实测通过，完整 WebUI 模式仍只验证
  `1.41.1 (84131)`。
- 新增 GitHub Actions 静态检查、arm64 构建和标签预发布流程。

## 0.1.0-rc.1

- 首个内部候选包，建立 Build 84131 的 87 条 WebUI 规则与 180 文件哈希基线。
- 菜单动态库和完整辅助进程重签名当时仍需手工执行，未公开发布。
